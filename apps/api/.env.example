PORT=3000
CORS_ORIGIN=http://localhost:5173
DATABASE_URL=postgres://iptvflix:iptvflix@localhost:5432/iptvflix
NODE_ENV=production
TMDB_API_KEY=
TMDB_STALE_DAYS=7
AUTH_USERNAME=admin
AUTH_PASSWORD_HASH=<bcrypt hash — generate with: node -e "require('bcryptjs').hash('your-pw',12).then(console.log)">
JWT_SECRET=<random 64+ char string>
# Optional: external HTTPS media relay (Fly.io / VPS). Both required to enable.
# MEDIA_RELAY_URL=https://iptvflix-media-relay.fly.dev
# MEDIA_RELAY_SECRET=<same secret as on the relay>
