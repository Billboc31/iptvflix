{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "pnpm --filter recommendation-engine start:railway"
  },
  "steps": {
    "build": {
      "commands": ["...", "pnpm --filter recommendation-engine build"]
    }
  }
}
