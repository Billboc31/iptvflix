import type { EpisodeResponse, DeviceResponse } from '@iptvflix/api-contracts';
type Props = {
    episode: EpisodeResponse;
    devices?: DeviceResponse[];
    progressMs?: number;
};
export default function EpisodeCard({ episode, devices, progressMs }: Props): import("react").JSX.Element;
export {};
//# sourceMappingURL=EpisodeCard.d.ts.map