Implementation complete. Here's a summary:

## Files created or modified

**New files:**
- `apps/api/src/db/schema/discovery-candidates.ts` — `discovery_candidates` table with unique `(external_id, media_type)`, two indexes, FK → movies/series
- `apps/api/src/services/discovery-candidate-pool-service.ts` — `DiscoveryCandidatePoolService` with `refreshPool`, `evictStale`, `materializeCandidate`
- `apps/api/src/services/__tests__/discovery-candidate-pool-service.test.ts` — 13 tests covering all 10 plan scenarios
- `apps/api/migrations/0018_discovery_candidates.sql` — generated by `drizzle-kit generate`
- `apps/api/migrations/meta/0018_snapshot.json` — generated snapshot

**Modified files:**
- `apps/api/src/providers/metadata/types.ts` — added `DiscoveryFeed` type, `fetchMovieFeed`/`fetchSeriesFeed` to `MetadataProvider` and `NoopMetadataProvider`, `popularity`/`voteAverage` to `MetadataCandidate`
- `apps/api/src/providers/metadata/tmdb/types.ts` — added `popularity`/`vote_average` to `TmdbSearchResultItem`
- `apps/api/src/providers/metadata/tmdb/client.ts` — implemented `fetchMovieFeed`/`fetchSeriesFeed`, updated search mappers
- `apps/api/src/db/schema/index.ts` — re-exported `discovery-candidates`
- `apps/api/src/services/__tests__/external-discovery-service.test.ts`, `metadata-enrichment-service.test.ts`, `title-matching-service.test.ts` — added the two new required methods to provider mock factories

**Known limits:**
- The pre-existing `title-matching-service.test.ts` integration failure (DB has stale `tmdbId=603`) is unrelated to T038 and was present before this branch.
- Cross-reference (`canonicalMovieId`/`canonicalSeriesId`) uses raw SQL `db.execute()` — fully verified structurally in tests but exercised end-to-end only against a real DB.
