All 415 tests pass (16 new). Here's a summary of all files created or modified:

**New files:**
- `apps/api/src/db/schema/explicit-feedback.ts` — Drizzle table with `feedbackTypeEnum` (`LIKE | DISLIKE | NOT_INTERESTED`) and `explicitFeedback` table (unique on profileId+mediaType+mediaId, FK cascade, createdAt+updatedAt)
- `apps/api/src/services/feedback-service.ts` — `upsertFeedback` (validates media, `onConflictDoUpdate`), `removeFeedback`, `listFeedback`
- `apps/api/src/routes/feedback.ts` — `GET /feedback`, `PUT /feedback/:mediaType/:mediaId`, `DELETE /feedback/:mediaType/:mediaId`
- `apps/api/src/routes/__tests__/feedback.test.ts` — 16 tests covering all transitions, idempotency, validation, and isolation
- `apps/api/migrations/0016_silent_lucky_pierre.sql` — adds `feedback_type` enum and `explicit_feedback` table only (auto-generated then trimmed to avoid pre-existing snapshot drift)
- `apps/api/migrations/meta/0016_snapshot.json` — generated snapshot (complete schema state)
- `apps/web/src/hooks/useFeedback.ts` — optimistic-update hook matching `useWatchlist` pattern
- `apps/web/src/components/content/FeedbackButtons.tsx` — three toggle buttons (LIKE/DISLIKE/NOT_INTERESTED) with `aria-pressed` state

**Modified files:**
- `apps/api/src/db/schema/index.ts` — added `explicit-feedback` export
- `packages/api-contracts/src/user-state.ts` — added `FeedbackType`, `SetFeedbackBody`, `FeedbackItem`
- `apps/api/src/index.ts` — registered `feedbackRoutes`
- `apps/web/src/lib/api.ts` — added `fetchFeedback`, `setFeedback`, `clearFeedback`
- `apps/web/src/pages/MovieDetailPage.tsx` — added `<FeedbackButtons mediaType="MOVIE" mediaId={movie.id} />`
- `apps/web/src/pages/SeriesDetailPage.tsx` — added `<FeedbackButtons mediaType="SERIES" mediaId={series.id} />`

**Note on migration**: The auto-generated SQL included extra table creations (`shelves`, `shelf_members`, `release_event_media_type`) due to a pre-existing snapshot drift from migration 0008 (hand-crafted). I trimmed the SQL to only what T039 introduces; the 0016 snapshot (generated by drizzle-kit) is kept intact as it correctly represents the full schema state.
