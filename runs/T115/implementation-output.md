All 26 tests pass. Here's the summary of changes made:

**Files modified:**

1. **`apps/api/src/services/metadata-enrichment-service.ts`** — Added `eq(movies.matchStatus, 'MATCHED')` and `eq(series.matchStatus, 'MATCHED')` to the `enrichPending()` query filters (lines 613, 623). This fixes the bug where repeated refresh runs would revisit non-matched rows that can never be enriched, causing the same population to be processed indefinitely.

2. **`apps/api/src/routes/catalog-enrich-missing.ts`** — Added input validation for `POST /admin/catalog-enrich-missing`: `batchSize` (1–500), `concurrency` (1–20), `throttleMs` (≥ 0). Returns 400 with a descriptive error message on invalid input.

**Remaining blocker (requires human action):** The production run artifact (`runs/T115/production-run-YYYYMMDD.md`) still needs to be generated by running the enrichment against production or a production DB snapshot. This cannot be satisfied from the AI development environment.
